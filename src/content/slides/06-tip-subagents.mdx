<TipSlide
  title="Leverage Sub-agents"
  description="Claude can spawn sub-agents to handle research or parallel tasks without cluttering the main context."
  tags={["Architecture", "Advanced"]}
>

```markdown
<!-- In your prompt -->
Use a sub-agent to search the codebase for
all usages of the deprecated `loadConfig` function.
```

</TipSlide>
