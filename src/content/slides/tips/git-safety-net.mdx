<TipSlide
  title={"Git as a Safety Net"}
  description={"Commit frequently with Git rather than relying on Claude Code's context rewind."}
  tags={["Workflow", "Gotcha"]}
>

- **Prefer Git over rewind:** Commit frequently with Git rather than relying on Claude Code's context rewind. Git is more reliable, familiar, and inspectable.
- **Commit after each step:** Commit after each meaningful step. Ask Claude to write commit messages using your templates.

</TipSlide>
